kind: classification
dataset_id: theatticusproject/cuad-qa
max_length: 512

training:
  pilot_epochs: 1
  larger_epochs: 2
  batch_size: 2
  gradient_accumulation_steps: 8
  learning_rate: 2.0e-4
  warmup_ratio: 0.05
  weight_decay: 0.01
