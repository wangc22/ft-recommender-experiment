# NOTE: Colab pre-installs torchvision/torchaudio bound to its own torch version.
# We do NOT pull in torchvision/torchaudio (this project is text-only); but Colab
# will already have them and they ABI-break when torch is upgraded. Install
# script (scripts/colab_bootstrap.sh) uninstalls them first.
torch==2.8.0
transformers==4.57.6
trl==0.24.0
peft==0.17.1
datasets>=3.0.0,<4.0.0
huggingface-hub>=0.28.0
accelerate>=1.0.0
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
pyyaml>=6.0
psutil>=5.9.0
matplotlib>=3.9.0
seaborn>=0.13.0
scipy>=1.13.0
rouge-score>=0.1.2
sacrebleu>=2.4.0
anthropic>=0.40.0
bitsandbytes>=0.43.0  ; sys_platform == "linux"

